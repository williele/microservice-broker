serializer:
  name: 'arvo'
transporter:
  name: 'nats'
  options:
    servers:
      - http://localhost:4444

services:
  nest-gateway:
    generate:
      dir: apps/nest-gateway/src/app/shared
      forNest: true
      dependencies:
        nest:
