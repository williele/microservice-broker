sources:
  default:
    serializer: 'arvo'
    transporter:
      name: 'nats'
      options:
        servers:
          - 'http://localhost:4444'

services:
  nest_gateway:
    serviceName: 'gateway'
    generate:
      output: 'node_modules/.broker/nest_gateway'
    dependencies:
      nest_service:
        alias: 'nest'

  nest_service:
    serviceName: 'nest'
    schema: 'apps/nest-service/service-schema.json'

  demo:
    serviceName: 'bar'
    generate:
      output: 'node_modules/.broker/demo'
    dependencies:
      nest_service:
        alias: 'nest'
